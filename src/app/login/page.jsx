import LoginLayer from "@/modules/auth/pages/Login";

export const metadata = {
  title: "Login",
  description:
    "login to apps.",
};

const Page = () => {
  return (
    <>
        <LoginLayer />
    </>
  );
};

export default Page;
